import { defineConfig } from 'vite'

// https://vitejs.dev/config/
export default defineConfig({
    // Plugin 'react' is removed because this is a Vanilla JS project
    base: './', // Important for GitHub Pages deployment
})
